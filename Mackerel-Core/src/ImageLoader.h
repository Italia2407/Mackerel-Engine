#pragma once

//#include "stb_image.h"
//#include "imgui.h"

#include <string>

namespace MCK::UI
{
	struct Image
	{
		unsigned int id = 0;
		//ImTextureID textureID = nullptr;
	};

	//class ImageLoader
	//{
	//public:
		//Image LoadImage(const std::string& filePath); 
		//void UnloadImage(Image& image);
	//};
}
